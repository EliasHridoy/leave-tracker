{
  "name": "leave-tracker",
  "version": "0.1.0",
  "packageManager": "yarn@4.6.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "tauri": "tauri",
    "dev": "env -u GTK_PATH -u GTK_EXE_PREFIX -u GTK_MODULES -u GDK_PIXBUF_MODULE_FILE -u GDK_PIXBUF_MODULEDIR -u GSETTINGS_SCHEMA_DIR -u GIO_MODULE_DIR -u XDG_DATA_HOME -u LD_LIBRARY_PATH tauri dev"
  },
  "private": true,
  "dependencies": {
    "@angular/common": "^20.1.4",
    "@angular/compiler": "^20.1.4",
    "@angular/core": "^20.1.4",
    "@angular/forms": "^20.1.4",
    "@angular/platform-browser": "^20.1.4",
    "@angular/router": "^20.1.4",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "rxjs": "~7.8.2",
    "tslib": "^2.8.1",
    "zone.js": "~0.15.1"
  },
  "devDependencies": {
    "@angular/build": "^20.1.4",
    "@angular/cli": "^20.1.4",
    "@angular/compiler-cli": "^20.1.4",
    "@tauri-apps/cli": "^2",
    "typescript": "~5.8.3"
  }
}
